programa "cores":
inicio
    x = ler_varios(5, 3, 1)
    y = ler()
    z = ler()
    se ((y == 11) e (z > 12)) ou x entao
    inicio
        mostrar(3)
    fim
    y = ler()
    esperar(1*1000)
    mostrar_tocar(y%10, (y+1) % 10)
fim.